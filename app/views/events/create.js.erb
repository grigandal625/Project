$("<%= escape_javascript(render @event) %>").appendTo("#addTo");
$('div.events').draggable({
      containment: 'table',
      revert : 'invalid'
});
$('#addTo').removeAttr('id');
$('#download').fadeOut(400);