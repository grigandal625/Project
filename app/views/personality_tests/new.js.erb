var $li = $(' <%= escape_javascript render( partial: 'list_test', locals: {test: @test, admin: true}) %> ');
$('#pt_main').html(' <%= escape_javascript render( partial: 'edit' ) %> ');
$('#pt_add_test').before($li);
$('#pt_tests .active').removeClass('active');
$li.find('a').addClass('active');
$('.x-editable').editable({
    validate: function(value) {
        if($.trim(value) == '') {
            return 'Пустая строка недопустима';
        }
    }
});