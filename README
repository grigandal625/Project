1)чтобы добавить редактор БНФ на страницу, подключи файлы BNF.css и BNF.js, а затем вызови функцию initBNF. Функция принимает два параметра: первый - массив массивов строк(элементы БНФ), второй - div, в котором будет создан редактор.
